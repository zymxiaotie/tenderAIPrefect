# TenderAI Configuration
# Copy to .env and fill in your values

# ============================================================================
# OpenAI Configuration (ChatGPT)
# ============================================================================
OPENAI_API_KEY=sk...
# OPENAI_API_KEY=sk-proj-your-key-here
LLM_PROVIDER=openai
LLM_MODEL=gpt-4o-mini

GEMINI_API_KEY=AI...
XAI_API_KEY=xai...
OPENAI_MODEL=gpt-4o                     # Options: gpt-4o, gpt-4-turbo, gpt-3.5-turbo
OPENAI_MAX_TOKENS=2000
OPENAI_TEMPERATURE=0.3

# ============================================================================
# PostgreSQL Database
# ============================================================================
DATABASE_URL=postgresql://postgres:passwordhere@db.uitxesbunncxtmpmvdvi.supabase.co:5432/postgres
DB_POOL_MIN=1
DB_POOL_MAX=10
DB_TIMEOUT=30

# Alternative individual parameters
DB_HOST=db.uitxesbunncxtmpmvdvi.supabase.co
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=yourpasswordhere

# ============================================================================
# Google Drive Configuration
# ============================================================================
GOOGLE_CREDENTIALS_FILE=credentials.json
GOOGLE_DRIVE_FOLDER_ID=1aRu-Rnc7vShNYrbV2yoRt2TN1mlaxwCQ
GOOGLE_SCOPES=https://www.googleapis.com/auth/drive.readonly


# ============================================================================
# Processing Configuration
# ============================================================================
POLLING_INTERVAL=300                    # Seconds between Google Drive scans
TAG_SIMILARITY_THRESHOLD=0.85           # 0.80-0.90 for tag normalization
CHUNK_SIZE=800                          # Text chunk size for embeddings
CHUNK_OVERLAP=150                       # Overlap between chunks
MAX_RETRIES=3                           # Retry failed operations
RETRY_DELAY_MINUTES=30

# ============================================================================
# Embedding Model
# ============================================================================
EMBEDDING_MODEL='BAAI/bge-m3'            # Sentence transformer model
EMBEDDING_CACHE_DIR='.cache/embeddings'

# ============================================================================
# Logging
# ============================================================================
LOG_LEVEL=INFO                          # DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_FILE='logs/tenderai.log'
LOG_FORMAT='%(asctime)s - %(name)s - %(levelname)s - %(message)s'
LOG_ROTATION=10MB                       # Rotate when file reaches size
LOG_BACKUP_COUNT=5

# ============================================================================
# Flask API (Optional)
# ============================================================================
FLASK_PORT=5001
FLASK_HOST=0.0.0.0
FLASK_DEBUG=false
FLASK_SECRET_KEY=your-secret-key-here

# ============================================================================
# Email delivery
# ============================================================================
EMAIL_SMTP_HOST=smtp.gmail.com
EMAIL_SMTP_PORT=587
EMAIL_USERNAME=zymxiaotiezym@gmail.com
EMAIL_PASSWORD=H***
EMAIL_FROM=skills.apex@proton.me
EMAIL_RECIPIENTS=zymxiaotie@gmail.com
EMAIL_USE_TLS=true
EMAIL_SUBJECT_PREFIX=Tender Intelligence Report


# ============================================================================
# Feature Flags
# ============================================================================
ENABLE_AUTO_QUALIFICATION=true
ENABLE_AUTO_REPORTS=true
ENABLE_EMAIL_ALERTS=true
ENABLE_API=false
